// controllers/tasks.js
const getAllTasks = (req, res) => {
    res.send('please motherfucker please mother fucker work'); // Cambia esto a lo que quieras que se muestre
};

module.exports = {
    getAllTasks,
};
